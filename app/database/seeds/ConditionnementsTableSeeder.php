<?php

class ConditionnementsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('conditionnements')->truncate();

		$conditionnements = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('conditionnements')->insert($conditionnements);
	}

}
