<?php

class AdressesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('adresses')->truncate();

		$adresses = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('adresses')->insert($adresses);
	}

}
